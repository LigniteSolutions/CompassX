name: CompassX
description: A Compass and weather showing app.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^0.1.3
  http: ^0.12.1
  sensors: ^0.4.2+2
  flare_flutter: ^2.0.5
  flutter_compass: ^0.4.2+1
  location: ^3.0.2
  firebase_admob: ^0.9.3+2

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  assets:
    - assets/
